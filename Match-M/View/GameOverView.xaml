<UserControl
    x:Class="Match_M.View.GameOverView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:model="clr-namespace:Match_M.Model"
    xmlns:vm="clr-namespace:Match_M.ViewModel"
    d:DataContext="{d:DesignInstance Type=vm:GameOverViewModel}"
    d:Height="{x:Static model:GameConstants.SCREEN_HEIGHT}"
    d:Width="{x:Static model:GameConstants.SCREEN_WIDTH}"
    mc:Ignorable="d">

    <Grid Background="#222">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="1"
            Margin="0,0,0,40"
            HorizontalAlignment="Center"
            FontSize="48"
            Foreground="White"
            Text="Game Over" />

        <Button
            Grid.Row="2"
            Width="120"
            Height="50"
            HorizontalAlignment="Center"
            Command="{Binding GameOverOkCommand}"
            Content="OK"
            FontSize="22" />
    </Grid>
</UserControl>
